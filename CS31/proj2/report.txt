i kept on getting the flag for coffee as true for some reason,even though my logic was correct adn identical to the other code that was the same exact. I fixed this by init
if name = b=nymber
round if edge case?  
